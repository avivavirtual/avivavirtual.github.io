services:
  whisper:
    build: ./apps/whisper
    env_file: ./apps/whisper/.env.example
    ports: ["8000:8000"]
    volumes: ["whisper_models:/app/models"]
    deploy:
      resources:
        limits:
          memory: 4G
volumes:
  whisper_models:
