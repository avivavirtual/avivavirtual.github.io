import { DashboardPageContent } from "@/components/dashboard/PageScaffold"

export default function TicketsPage() {
  return <DashboardPageContent title="Tickets" subtitle="Filter by status, priority, assigned agent, and due date." />
}
